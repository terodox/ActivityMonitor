<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/app_selector_list_item_thumbnail"
        style="@style/AppIcon"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"/>

    <!-- Title Of Song-->
    <TextView
        android:id="@+id/app_selector_list_item_appName"
        style="@style/HeaderText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_alignTop="@id/app_selector_list_item_thumbnail"
        android:layout_toRightOf="@id/app_selector_list_item_thumbnail"
        android:text="Empty Item Text" />

</RelativeLayout>